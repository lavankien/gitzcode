#include <bits/stdc++.h>


using namespace std;

using ll = long long;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    cin.ignore();
    while(t--) {
   		int n,k;
   		cin>>n>>k;
   		long a[1000000];
   		int d=0;
   		vector <int> k;
   		for (int i=0; i<n; i++){
   				cin>>a[i];
   				k.push_back(a[i]-k)
		   }
		
    }
    return 0;
}
